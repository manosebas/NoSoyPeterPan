# deps
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
build/
*.tsbuildinfo

# entornos locales: este proyecto NO corre en local.
# Todas las variables viven en Vercel y Railway.
.env
.env.*
!.env.example

# tooling
.turbo/
.vercel/
coverage/

# so
.DS_Store
Thumbs.db

# mcp: lleva el access token de Supabase, jamas al repo.
.mcp.json

# proceso privado: documentacion interna, SQL, notas del agente y material
# de trabajo. Viven solo en local.
# Anclados a la raiz: sin la `/` inicial tambien se ignoraria
# apps/web/src/lib/supabase/, que es codigo.
/docs/
/supabase/
/otros/
/CLAUDE.md
CLAUDE.local.md
.claude/
