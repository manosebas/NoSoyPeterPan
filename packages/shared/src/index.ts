export * from './domain.js';
export * from './http.js';

// La frase de oro vivia aqui como una sola cadena. El manifiesto de ahora tiene
// parrafos y un cierre en negrita, y se escribe solo al llegar a su seccion: su
// forma es parte del texto, asi que vive en `web/components/FraseEscrita.tsx` y
// no en una constante. Duplicarlo aqui solo garantizaba que se desincronizaran.
